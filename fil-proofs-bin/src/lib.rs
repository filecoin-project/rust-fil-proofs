//! This library should *not* be used outside of this project. Those modules are not bound to
//! semantic versioning. Things might change arbitrarily even within patch releases. The modules
//! are only public as it's the only way to share code between binaries.

#[doc(hidden)]
pub mod cli;
